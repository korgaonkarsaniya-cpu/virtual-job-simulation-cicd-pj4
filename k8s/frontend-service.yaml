apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: virtual-job-platform
spec:
  selector:
    app: virtual-job-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
